const Home = async () => {
  const view = /*html*/ `
    <div class="container default-column center">
      <h2>Bienvenido</h2>
      <p>
        Selecciona una opción del menú para ver el contenido
        correspondiente y poder probar los algoritmos en tiempo real.
      </p>
    </div>
  `;
  return view;
};

export default Home;
