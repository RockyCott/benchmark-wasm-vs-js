const Home = async () => {
  const view = /*html*/ `
    <div class="container default-column center">
      <h2>Bienvenido</h2>
      <p>
        Selecciona una opción del menú de la parte izquierda para ver el contenido
        correspondiente.
      </p>
    </div>
  `;
  return view;
};

export default Home;
